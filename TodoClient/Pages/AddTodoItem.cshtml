@page
@model AddTodoItemModel

<div class="container">
    <div class="row">
        <div asp-validation-summary="All"></div>
    </div>
    <div class="row">
        @using (Html.BeginForm("AddTodoItem", "AddTodoItem", FormMethod.Post))
        {
            @*<div class="form-group">
                @Html.EditorFor(x => Model.TodoItem.Name)
            </div>
            <div class="form-group">
                @Html.LabelFor(x => x.TodoItem.IsComplete, new { @class = "label-grey" })
                @Html.CheckBoxFor(x => Model.TodoItem.IsComplete)
            </div>
            @Html.LabelFor(x => x.TodoItem.User, new { @class = "label-grey h4" })
            <div class="form-group">
                @Html.EditorFor(x => Model.TodoItem.User.FirstName)
                @Html.EditorFor(x => Model.TodoItem.User.LastName)
            </div>*@

            @await Component.InvokeAsync("EditTodoItem", new { todoItem = @Model.TodoItem})
            <input class="btn btn-success" id="btnSave" type="submit" asp-page-handler="SaveTodo" value="Gem" />
        }
    </div>
</div>